/**
 * Indicates that the waiting for the loop to finish is over and no valid results were approved
 */
class MaximumRepetitionsError extends Error {
    constructor(message, repetitions) {
        super(message);
        this.repetitions = repetitions;
    }
}

module.exports = {
	MaximumRepetitionsError
};
